我现在希望编写一个python 库，支持TagCall，解析 类似于 
<function-call>
get_weather("北京");
add(1+3);
</functon-call>
这种的里面的内容，然后一行一行的解析。
同时我希望能注册方法
@function_call(<方法提示词说明>)
def  aaa(aaa，bbb){


}
被这个function_call 装饰器代码，我希望能向一个地方注册，注册的数据结构大概是这样的
{
    方法名称：{
        prompt:"方法提示词说明",
        function_str:"方法字符串"
        function:真实的方法。
    }

}
这个库的主要目标是 向系统提示词里面注入  这些 被 function_call 标记的 方法的prompt 和 function_str 。然后大模型如果需要调用这些方法，就用<function_call></function_call> 标记返回。里面一行一条指令。
